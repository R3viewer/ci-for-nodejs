function sum (a, b) {
  return Number(a) + Number(b)
}

function substract (a, b) {
  return a - b
}

function multiple (a, b) {
  return a * b
}

function divide (a, b) {
  return a / b
}

export default {
  sum,
  substract,
  multiple,
  divide
}
